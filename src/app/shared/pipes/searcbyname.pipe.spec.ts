import { SearcbynamePipe } from './searcbyname.pipe';

describe('SearcbynamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearcbynamePipe();
    expect(pipe).toBeTruthy();
  });
});
