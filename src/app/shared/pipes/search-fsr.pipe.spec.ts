import { SearchFsrPipe } from './search-fsr.pipe';

describe('SearchFsrPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFsrPipe();
    expect(pipe).toBeTruthy();
  });
});
